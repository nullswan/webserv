server {
	server_name	webserv;

	root		tests/www/html;
	autoindex 	on;

	location /ping {
		allowed_methods	GET;
		index			ping.html;
	}

	location /files {
		autoindex	on;
		root		tests/www/html;
	}
}