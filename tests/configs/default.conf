server {
	server_name	webserv;

	index		index.html index.php;
	root		test/www/html;

	location /ping {
		allowed_methods	GET;
		index			ping.html;
	}

	location /files {
		autoindex	on;
		root		test/www/html;
	}
}